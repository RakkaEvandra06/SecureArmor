# Drop a SecLists-style wordlist here as common_passwords.txt (one password per line).
# When absent, the loader falls back to the small built-in list in
# passcheck/constants/common_passwords_data.py and logs a warning.
